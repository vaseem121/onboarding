@model smartTechAuthenticator.ViewModels.CustomerInfoViewModel
@{
    ViewBag.Title = "CompanyDetails";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<style>
    button.btn.btn-primary {
        margin-right: 5px;
    }

    .card-body:before, .card-body:after {
        content: "";
        display: block;
        clear: both;
    }
</style>
<div class="page-breadcrumb">
    <div class="row">
        <div class="col-12 d-flex no-block align-items-center">
            <h4 class="page-title">Manage Company</h4>
            <div class="ms-auto text-end">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="@Url.Action("Products", "Customer")">Manage Company</a></li>
                        <li class="breadcrumb-item active" aria-current="page">
                            Manage Company
                        </li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-6">
            <div class="card">
                @using (Html.BeginForm("ProductDetails", "Customer", FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal" }))
                {

                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(x => x.Id)
                <div class="card-body">
                    <h4 class="card-title">Product Information</h4>
                   <div class="form-group row">
                        @Html.LabelFor(x => x.Name, new { @class = "col-sm-3 text-end control-label col-form-label" })
                        <div class="col-sm-9">
                            @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                            @Html.ValidationMessageFor(x => x.Name, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group row">
                        @Html.LabelFor(x => x.Location, new { @class = "col-sm-3 text-end control-label col-form-label" })
                        <div class="col-sm-9">
                            @Html.TextBoxFor(x => x.Location, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group row">
                        @Html.LabelFor(x => x.Website, new { @class = "col-sm-3 text-end control-label col-form-label" })
                        <div class="col-sm-9">
                            @Html.TextBoxFor(x => x.Website, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group row">
                        @Html.LabelFor(x => x.Email, new { @class = "col-sm-3 text-end control-label col-form-label" })
                        <div class="col-sm-9">
                            @Html.TextBoxFor(x => x.Email, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group row">
                        @Html.LabelFor(x => x.MobileNo, new { @class = "col-sm-3 text-end control-label col-form-label" })
                        <div class="col-sm-9">
                            @Html.TextBoxFor(x => x.MobileNo, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group row">
                        @Html.LabelFor(x => x.UserPass, new { @class = "col-sm-3 text-end control-label col-form-label" })
                        <div class="col-sm-9">
                            @Html.TextBoxFor(x => x.UserPass, new { @class = "form-control" })
                        </div>
                    </div>







                </div>
                    <div class="">
                        <div class="card-body">
                           
                            <a href="@Url.Action("Products","Customer" )" class="btn btn-danger" style="float: right;">
                                Cancel
                            </a>
                            <button type="submit" class="btn btn-primary" style="float: right;">
                                Update
                            </button>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
<script src="~/Content/assets-admin/libs/jquery/dist/jquery.min.js"></script>
<script type="text/javascript">
    $(document).ready(function () {
        var a = $('#protype').val();
        if (a == "2") {
            $(".colrsize").css("display", "flex");
        }

        $("#protype").change(function () {
            var a = $('#protype').val();
            alert(a);
            if (a == '2') {
                $(".colrsize").css("display", "flex");
            }
            else {
                $(".colrsize").css("display", "none");
            }
        })

    });
    var counter2 = 1;
    $(function () {
        $('#Mainadd').click(function () {
            $(' <div class="row" id = "tableMainrow0' + counter2 + '">' +
                ' <div class= "col-md-6" > ' +
                '<div class= "form-group" > ' +
                '<input class="form-control" id="Size" name="Size" type="number" >' +
                '</div></div>' +
                '<div class="col-sm-3" id="AddMainTableAdd">' +
                '<button id="MainaddAdd" class="btn btn-danger" type="button" onclick="removeMainTr(this);">Delete</button>' +
                '</div>' +
                ' </div>').appendTo('#MainSizeAdd');
            counter2++;
            return false;
        });
    });

    var counter2 = 1;
    $(function () {
        $('#MainaddAdd').click(function () {
            $(' <div class="row" id = "NewRow0' + counter3 + '">' +
                ' <div class= "col-md-6" > ' +
                '<div class= "form-group" > ' +
                '<input class="form-control" id="Color" name="Color" type="color" >' +
                '</div></div>' +
                '<div class="col-sm-3" id="AddMainTableAdd">' +
                '<button id="MainaddAdd" class="btn btn-danger" type="button" onclick="removeMainTr(this);">Delete</button>' +
                '</div>' +
                ' </div>').appendTo('#MainsubmissionTableAdd');
            counter1++;
            return false;
        });
    });
    function removeMainTr(index) {
        debugger;
        //  if (index > 1) {
      //  console.log(this);
        $(index).parent().parent("div").remove();
        //  $('#tableMainr_' + index).remove();
        // counter1--;
        //  }
        return false;
    }


    var counter3 = 1;
    $(function () {
        $('#tag').click(function () {
            $(' <div class="row" id = "tableMainrow0' + counter2 + '">' +
                ' <div class= "col-md-6" > ' +
                '<div class= "form-group" > ' +
                '<input class="form-control" id="ProductTag" name="ProductTag" type="text" requaired >' +
                '</div></div>' +
                '<div class="col-sm-3" id="AddMainTableAdd">' +
                '<button id="MainaddAdd" class="btn btn-danger" type="button" onclick="removetag(this);">Delete</button>' +
                '</div>' +
                ' </div>').appendTo('#ProductTag');
            counter1++;
            return false;
        });
    });

    function removetag(index) {
        debugger;
        //  if (index > 1) {
       // console.log(this);
        $(index).parent().parent("div").remove();
        //  $('#tableMainr_' + index).remove();
        // counter1--;
        //  }
        return false;
    }

</script>